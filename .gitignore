node_modules/
dist/
dist-desktop/
dist-ssr/
*.local
.DS_Store
.vite/

# Tauri (desktop admin) build artifacts — Rust target + generated files.
src-tauri/target/
src-tauri/gen/

# Offline map packs are large binaries — generated by the agency, not committed by default.
# (Remove this line if you intentionally commit small regional packs under public/data/packs/.)
public/data/packs/*.pmtiles
